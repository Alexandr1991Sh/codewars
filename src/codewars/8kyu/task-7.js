function sumMul(n, m) {
    if (n < 1 || m < 1) return 'INVALID'
    let result = 0

    for (let i = 0; i < m; i++) {

    }

    return result
}

console.log(sumMul(0, 0))            //  'INVALID'
console.log(sumMul(2, 9))            // 20  (2 + 4 + 6 + 8)
console.log(sumMul(4, -7))          //  'INVALID'
